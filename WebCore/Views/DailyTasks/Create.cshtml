@model WebApi.Models.DailyTask

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>DailyTask</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TaskRef" class="control-label"></label>
                <select asp-for="TaskRef" class="form-control" asp-items="ViewBag.TaskRef"></select>
            </div>
            <div class="form-group">
                <label asp-for="EmployeeRef" class="control-label"></label>
                <select asp-for="EmployeeRef" class="form-control" asp-items="ViewBag.EmployeeRef"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
